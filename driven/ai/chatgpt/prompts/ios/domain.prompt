Genera las clases de dominio necesarias para representar las entidades en una arquitectura clean en swift.
Además de estas entidades genera una clase por dominio con los casos de uso asociados a estas y su implementación,
todos los casos de uso deben de estar agrupados por dominio.
Crea el protocolo para el repositorio que tiene que tener el nombre acabado en Protocol.
Cada uno de las clases o protocolos deben de estar en un bloque de código distinto. Encima del bloque tiene que
indicarse el nombre del fichero (###DIRECTORIO/NOMBREFICHERO) y la carpeta a la que corresponde. Las carpetas son:
Domain/Entities, Domain/UseCases, Domain/Repositories
Quiero que todas las funciones sean con async await.